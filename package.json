{
  "name": "actor-vault",
  "version": "1.2.12",
  "private": true,
  "description": "Foundry VTT v13 actor vault module",
  "scripts": {
    "validate": "node scripts/validate-package.mjs",
    "build": "node scripts/build-release.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
